<h1>All Users:</h1>
<% @users.each do |user| %>
<p><%= link_to user.email, user_path(user) %> | 
<%= link_to "delete", user_path(user), method: :delete,
	      data: { confirm: 'Are you sure?'} %></p>
<% end %>
